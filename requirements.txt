# requirements.txt (Slimmed for Vercel Serverless)

fastapi
uvicorn[standard]
transformers>=4.40.0 # Pinning transformers slightly just in case
# Install CPU-only PyTorch (much smaller)
torch --index-url https://download.pytorch.org/whl/cpu
sentencepiece
accelerate # Still potentially useful even on CPU for some things

# Removed torchvision, torchaudio, pandas, datasketch as likely not needed for API runtime
# python-dotenv # Keep if you plan to use .env files later